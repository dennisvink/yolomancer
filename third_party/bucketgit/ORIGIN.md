Selected S3, repository, and transport packages from BucketGit commit
5a7371c (local release 1.4.2), by Dennis Vink. MIT license retained in LICENSE.
The release tag was not available upstream when integrated, so this is a
self-contained source snapshot, not a dependency on a local checkout.
Includes upstream storage/transport tests. GCS and the BucketGit CLI are omitted.
